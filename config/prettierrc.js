// Prettier configuration for the AI-powered salesperson chat system

module.exports = {
  // Use semicolons at the end of statements
  semi: true,
  // Use trailing commas where valid in ES5 (objects, arrays, etc.)
  trailingComma: 'es5',
  // Use single quotes instead of double quotes
  singleQuote: true,
  // Specify the line length that the printer will wrap on
  printWidth: 100,
  // Specify the number of spaces per indentation-level
  tabWidth: 2,
  // Indent lines with spaces, not tabs
  useTabs: false,
  // Print spaces between brackets in object literals
  bracketSpacing: true,
  // Put the > of a multi-line JSX element at the end of the last line instead of being alone on the next line
  jsxBracketSameLine: false,
  // Include parentheses around a sole arrow function parameter only when necessary
  arrowParens: 'avoid',
  // Use Linux line endings
  endOfLine: 'lf',
};